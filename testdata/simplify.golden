SELECT foo, bar
FROM [table];

ALTER TABLE t1
ADD INDEX index_name (`column`);

SELECT x != y;

SELECT `if`, `match`, `row_number`;

SELECT CAST(CASE
	WHEN TRUE THEN 42
	DEFAULT 0
END AS int(11))

SELECT
	x.bar bar,
	x.bar bar,
	x.bar bar,
	x.bar foo,
	x.bar bar
FROM foo x;

# Do not remove the 'AS' keyword here.
SELECT CAST(created AS date);
